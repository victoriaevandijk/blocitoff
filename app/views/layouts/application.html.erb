<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href='https://fonts.googleapis.com/css?family=Shadows+Into+Light' rel='stylesheet' type='text/css'>
  <title>Blocitoff</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class="welcome">
  <div class="container">
    <ul class="nav nav-tabs">
      <li><%= link_to "Home", root_url %></li>
      <li><%= link_to "About", 'welcome/about' %></li>
      <div class="pull-right user-info">
      <% if current_user %>
        Hello <%= link_to "#{current_user.email}", new_user_session_path %>! <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
      <% else %>
        <strong><%= link_to "Sign In", new_user_session_path %></strong> or
        <strong><%= link_to "Sign Up", new_user_registration_path %></strong>
      <% end %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
    </ul>
    <%= yield %>
  </div>
</body>
</html>